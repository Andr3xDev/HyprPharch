pragma Singleton

import QtQuick

/*!
    IndicatorsState — local state placeholder for the indicators module.
    Extend here if indicator visibility or configuration needs to be shared
    within the module (e.g. OSD timeout overrides).
*/
QtObject {
    id: root
}
